# Copy this file to .env: cp .env.example .env

# URL of the vets service, used on the server (+page.server.ts).
VET_SERVICE_API_URL=http://localhost:4000

# Same URL, but readable in the browser (components that fetch themselves).
# Only variables starting with PUBLIC_ are sent to the browser.
PUBLIC_VET_SERVICE_API_URL=http://localhost:4000
